import React, { useState, useEffect } from "react";

function DisplayPublicationsOfMyClasse({ publications }) {
  // const [publicationsOfMyClasse, setPublicationOfMyClasse] = useState([]);

  // useEffect(() => {
  //     const publiMyClasse = publications.filter(publi => )
  // }, []);

  return (
    <>
      {/* {publications
            .filter(publi => publi.user.id === userId)
            .map(publication => {
                return <div></div>
            })
        } */}
    </>
  );
}

export default DisplayPublicationsOfMyClasse;
